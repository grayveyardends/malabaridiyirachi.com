export function notFoundPage() {
  return `
  <div class="wrap narrow notfound">
    <h1>Page not found</h1>
    <p>That page doesn't exist — maybe the link was old. The good stuff is still here though.</p>
    <a class="btn btn-primary btn-lg" href="/#shop">Browse the products</a>
  </div>
`;
}
